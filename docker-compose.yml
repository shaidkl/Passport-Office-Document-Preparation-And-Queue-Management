services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: passport_db
      POSTGRES_USER: passport_app
      POSTGRES_PASSWORD: local-demo-database-password
    volumes:
      - passport_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U passport_app -d passport_db"]
      interval: 5s
      timeout: 5s
      retries: 12

  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_DEBUG: "true"
      DJANGO_SECRET_KEY: "local-compose-demonstration-only"
      DJANGO_ALLOWED_HOSTS: "127.0.0.1,localhost,0.0.0.0"
      DB_ENGINE: "django.db.backends.postgresql"
      DB_NAME: "passport_db"
      DB_USER: "passport_app"
      DB_PASSWORD: "local-demo-database-password"
      DB_HOST: "db"
      DB_PORT: "5432"
      EMAIL_BACKEND: "django.core.mail.backends.smtp.EmailBackend"
      EMAIL_HOST: "${EMAIL_HOST:-}"
      EMAIL_PORT: "${EMAIL_PORT:-587}"
      EMAIL_HOST_USER: "${EMAIL_HOST_USER:-}"
      EMAIL_HOST_PASSWORD: "${EMAIL_HOST_PASSWORD:-}"
      EMAIL_USE_TLS: "${EMAIL_USE_TLS:-true}"
      DEFAULT_FROM_EMAIL: "${DEFAULT_FROM_EMAIL:-}"
      PUBLIC_BASE_URL: "${PUBLIC_BASE_URL:-http://127.0.0.1:8000}"
      EMAIL_VERIFICATION_TTL_SECONDS: "${EMAIL_VERIFICATION_TTL_SECONDS:-300}"
      EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS: "${EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS:-60}"
      EMAIL_VERIFICATION_MAX_SENDS_PER_HOUR: "${EMAIL_VERIFICATION_MAX_SENDS_PER_HOUR:-5}"
      EMAIL_VERIFICATION_IP_MAX_SENDS_PER_HOUR: "${EMAIL_VERIFICATION_IP_MAX_SENDS_PER_HOUR:-20}"
      EMAIL_VERIFICATION_OTP_MAX_ATTEMPTS: "${EMAIL_VERIFICATION_OTP_MAX_ATTEMPTS:-5}"
      EMAIL_VERIFICATION_OTP_ATTEMPT_WINDOW_SECONDS: "${EMAIL_VERIFICATION_OTP_ATTEMPT_WINDOW_SECONDS:-900}"
      EMAIL_VERIFICATION_OTP_CLEANUP_INTERVAL_SECONDS: "${EMAIL_VERIFICATION_OTP_CLEANUP_INTERVAL_SECONDS:-5}"
      MALWARE_SCAN_ENABLED: "false"
      PERSONNEL_MFA_REQUIRED: "false"
      ESEWA_ENVIRONMENT: "sandbox"
    depends_on:
      db:
        condition: service_healthy

volumes:
  passport_postgres_data:
